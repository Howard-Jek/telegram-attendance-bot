{
  "name": "telegram-attendance-bot",
  "private": true,
  "description": "Dev-only: offline tests for the Apps Script backend. Nothing here is deployed.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
